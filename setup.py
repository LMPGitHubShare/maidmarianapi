from setuptools import setup

setup(
   name='maidmarianapi',
   version='0.0.1',
   description='Maid Marian API',
   author='LMP',
   author_email='masterlau4@hotmail.com',
   packages=['maidmarianapi'],  #same as name
   install_requires=['wheel'], #external packages as dependencies
)